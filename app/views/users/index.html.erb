<h1>All users</h1>

x<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= profile_image_for user %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>